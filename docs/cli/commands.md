---
title: CLI commands
description: "Short link to the generated command reference."
---

The full command table is generated from the CLI binary so it cannot drift from
the product.

→ **[CLI command reference](/docs/reference/cli-commands)**

For global options, exit codes, and CI patterns, see [Using the CLI](/docs/cli/usage).
