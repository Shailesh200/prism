{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Shailesh200/prism",
  "title": "Prism",
  "description": "Local-first repo intelligence for agents: DNA, health, blast radius, and Dispatch jobs.",
  "repository": {
    "url": "https://github.com/Shailesh200/prism",
    "source": "github"
  },
  "websiteUrl": "https://www.prismhq.in",
  "icons": [
    {
      "src": "https://www.prismhq.in/brand/prism-mark.png",
      "mimeType": "image/png",
      "sizes": ["128x128"]
    }
  ],
  "version": "1.1.14",
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "@repo-prism/mcp-server",
      "version": "1.1.14",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "positional",
          "valueHint": "workspace",
          "description": "Optional absolute path to the repository root. Defaults to the client's MCP roots (open folder), then host WORKSPACE_FOLDER_PATHS, then nearest git root from cwd."
        }
      ],
      "environmentVariables": [
        {
          "name": "PRISM_WORKSPACE",
          "description": "Optional override when the MCP client starts from a directory outside the project.",
          "format": "string",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "CURSOR_WORKSPACE",
          "description": "Optional host-interpolated project folder (Cursor ${workspaceFolder}). Used only when it contains a git root. Not locked like PRISM_WORKSPACE.",
          "format": "string",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "CURSOR_API_KEY",
          "description": "Optional override to spawn local Cursor SDK workers. Prefer prism init / first start_job (Cursor.auth.login). Do not put this in mcp.json for normal use.",
          "format": "string",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "PRISM_DISPATCH_ROLE",
          "description": "Set to worker to skip Intelligence tools and hide start_job / start_my_day / init. Dispatch job agents do not attach Prism MCP.",
          "format": "string",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ],
  "_meta": {
    "io.modelcontextprotocol.registry/publisher-provided": {
      "io.prismhq": {
        "docs": "https://www.prismhq.in/docs/mcp/install",
        "installConfig": "packages/mcp-server/mcp-install.json"
      }
    }
  }
}
