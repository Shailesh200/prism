/** Core package version + API level for surfaces. */
export const PRISM_CORE_VERSION = "0.0.0" as const;

/** Bump when public method signatures change incompatibly (pre-1.0 soft). */
export const PRISM_API_LEVEL = 1 as const;
