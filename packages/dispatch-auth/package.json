{
  "name": "@repo-prism/dispatch-auth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Prism Auth — OAuth broker handlers for Dispatch drivers",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo-prism/dispatch": "workspace:*"
  },
  "license": "MIT"
}
