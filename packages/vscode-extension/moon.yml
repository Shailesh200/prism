$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
layer: "library"
dependsOn:
  - core
  - shared
  - ui
  - app-shell
tasks:
  build:
    command: "bun run build"
    deps:
      - "core:build"
      - "ui:build"
      - "app-shell:build"
    inputs:
      - "src/**/*"
      - "scripts/**/*"
      - "tsconfig.json"
      - "package.json"
    outputs:
      - "dist"
      - "media"
  typecheck:
    command: "bun run typecheck"
    deps:
      - "core:build"
      - "ui:build"
      - "app-shell:build"
    inputs:
      - "src/**/*"
      - "tsconfig.json"
  test:
    command: "bun run test"
    deps:
      - "core:build"
      - "app-shell:build"
    inputs:
      - "src/**/*"
      - "vitest.config.ts"
  test-integration:
    command: "bun run test:integration"
    inputs:
      - "src/**/*"
      - "vitest.integration.config.ts"
  package-vsix:
    command: "bun run package:vsix"
    deps:
      - "~:build"
    inputs:
      - "src/**/*"
      - "scripts/**/*"
      - "package.json"
      - "README.md"
      - ".vscodeignore"
    outputs:
      - "*.vsix"
