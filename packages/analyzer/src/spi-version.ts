/**
 * Analyzer LanguagePlugin SPI major version.
 * Host rejects plugins whose spiVersion is outside [MIN, MAX].
 */
export const ANALYZER_SPI_VERSION = 1 as const;
export const ANALYZER_SPI_VERSION_MIN = 1 as const;
export const ANALYZER_SPI_VERSION_MAX = 1 as const;
