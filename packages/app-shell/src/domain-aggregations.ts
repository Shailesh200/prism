/**
 * Re-exports DomainScreen pure aggregations from `@repo-prism/shared`
 * (M-053 T-10). Prefer importing from shared in new code.
 */

export {
  fileStem,
  inboundDepCounts,
  lookupInbound,
  normalizeDepKey,
} from "@repo-prism/shared";
