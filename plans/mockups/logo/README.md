# Prism logo (locked)

Transparent PNG masters:

- `prism-mark.png` — faceted P mark (teal)
- `prism-lockup.png` — horizontal lockup, light
- `prism-lockup-dark.png` — horizontal lockup, dark

Sized / color variants: [`exports/`](./exports/)

Regen mark sizes from white source:

```bash
python3 generate_locked_variants.py
```

Do not redesign without owner approval — see [`../LOCKED.md`](../LOCKED.md).
